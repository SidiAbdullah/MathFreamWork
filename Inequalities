// function that return the minimum number of vector
int min(vector <int> &vNumbers){
    int minNumber = vNumbers[0]; // supposing it is the first
    for (int &num : vNumbers){
        if (minNumber > num) minNumber = num;
    }
    return minNumber;
}
